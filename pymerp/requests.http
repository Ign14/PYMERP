### Get Token (Keycloak ROPC)
POST {{KEYCLOAK_URL}}/realms/{{KEYCLOAK_REALM}}/protocol/openid-connect/token
Content-Type: application/x-www-form-urlencoded

grant_type=password&client_id={{KEYCLOAK_CLIENT_ID}}&username={{OIDC_USERNAME}}&password={{OIDC_PASSWORD}}

@access_token = {{response.body.$.access_token}}

### Health
GET {{BACKEND_BASE}}/actuator/health

### Products (protected)
GET {{BACKEND_BASE}}/api/v1/products
Authorization: Bearer {{access_token}}
X-Company-Id: {{COMPANY_ID}}
